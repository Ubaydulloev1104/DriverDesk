<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="DriverDesk.Pages.HistoryPage"
    Title="История заказов">

    <VerticalStackLayout Padding="12" Spacing="10">

        <!-- Вкладки -->
        <HorizontalStackLayout Spacing="8" HorizontalOptions="Center">
            <Button x:Name="AllOrdersButton"
                    Text="Все"
                    Clicked="OnAllOrdersTabClicked"
                    BackgroundColor="#e0e0e0" />
            <Button x:Name="CompletedOrdersButton"
                    Text="Завершённые"
                    Clicked="OnCompletedOrdersTabClicked" />
        </HorizontalStackLayout>

        <!-- Поиск -->
        <SearchBar x:Name="SearchBar"
                   Placeholder="Поиск по имени клиента..."
                   TextChanged="OnSearchTextChanged"/>

        <!-- Список заказов (группированный) -->
        <CollectionView x:Name="OrdersCollectionView"
                        IsGrouped="True"
                        SelectionMode="None"
                        Margin="0,4"
                        VerticalOptions="FillAndExpand">

            <!-- Заголовок группы: дата + кол-во -->
            <CollectionView.GroupHeaderTemplate>
                <DataTemplate>
                    <Frame BackgroundColor="#f0f6ff"
                           CornerRadius="8"
                           Padding="8"
                           Margin="0,6,0,4"
                           HasShadow="False">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <Label Text="{Binding Key}" FontAttributes="Bold" FontSize="16" />
                            <Label Grid.Column="1" Text="{Binding Count, StringFormat='Всего: {0}'}"
                                   FontSize="14" TextColor="Gray" HorizontalOptions="End" />
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.GroupHeaderTemplate>

            <!-- Элемент заказа -->
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame Padding="10" Margin="0,4"
                           CornerRadius="10"
                           BorderColor="LightGray"
                           HasShadow="True"
                           BackgroundColor="{Binding BackgroundColor}">
                        <Grid ColumnDefinitions="*,Auto" RowDefinitions="Auto,Auto,Auto" ColumnSpacing="8">
                            <VerticalStackLayout Grid.Column="0" Spacing="4">
                                <Label Text="{Binding CustomerName}" FontAttributes="Bold" FontSize="16"/>
                                <Label Text="{Binding CustomerPhone}" FontSize="13" TextColor="Gray"/>
                                <Label Text="{Binding Description}" FontSize="14"/>
                                <HorizontalStackLayout Spacing="8">
                                    <Label Text="{Binding PickupDateTime, StringFormat='{0:dd.MM.yyyy HH:mm}'}" FontSize="12" TextColor="DarkBlue"/>
                                    <Label Text="{Binding StatusText}" TextColor="{Binding StatusColor}" FontSize="12" FontAttributes="Italic"/>
                                </HorizontalStackLayout>
                            </VerticalStackLayout>

                            <!-- Действия справа (по желанию можно добавить кнопки) -->
                            <VerticalStackLayout Grid.Column="1" VerticalOptions="Center" Spacing="6">
                                <!-- Здесь можно добавлять кнопки звонка/удаления и т.д. -->
                            </VerticalStackLayout>
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <!-- Нижний счётчик -->
        <Label x:Name="TotalCountLabel"
               Text="Всего заказов: 0"
               FontAttributes="Bold"
               HorizontalOptions="End"
               Margin="0,6,0,0"/>
    </VerticalStackLayout>
</ContentPage>
